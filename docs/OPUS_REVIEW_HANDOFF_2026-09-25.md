# Independent review handoff — 25 September 2026

**To the Opus 5.5 reviewer:** Audit the implementation and its claims independently. Treat this document and the project's evidence files as assertions to verify, not as a substitute for source review or chain reads. Start with findings; do not merge the draft PR or describe the app as production ready merely because its tests pass.

## Review target

- Repository: `GiraeffleAeffle/rental-deposit-hackathon`; local checkout: `/Users/max/Code/rental-deposit-hackathon`.
- Draft [PR #1](https://github.com/GiraeffleAeffle/rental-deposit-hackathon/pull/1): `codex/deposit-to-investments` into `codex/hackathon-foundation`.
- Pinned base: `6a9c0c9357cbad5fae00ed8ee8c4dc40491ab747`; implementation commit: `2a1925ba9b449c0f92e8edf43820c5724dd8cde5`. Review the code diff with `git diff 6a9c0c9357cbad5fae00ed8ee8c4dc40491ab747...2a1925ba9b449c0f92e8edf43820c5724dd8cde5`. The PR is large (172 files, about 40,000 added lines), so identify critical paths first, then check that documentation and UI claims match them.
- The current review request is **read-only**. Do not sign, broadcast, deploy, spend test tokens, change provider credentials, or touch the user's account to verify these claims. The user may decide whether you or Codex continues implementation after your verdict.

Read [README](../README.md), [implementation status](IMPLEMENTATION_STATUS.md), [validation](VALIDATION.md), [staged onboarding](STAGED_SOLANA_ONBOARDING.md), the [Solana API guide](SOLANA_NATIVE_API.md), and [ADR 0009](adr/0009-stage-solana-escrow-setup-and-funding.md). This is a hackathon prototype with an intentionally undecided product name. The older Gnosis repository is a separate product; the PR targets this public repo.

## Claims to test against evidence

| Claim | Independent check and boundary |
| --- | --- |
| Solana staged custody works on devnet | Separate Privy accounts signed landlord-only empty setup, tenant funding, Kamino supply and redemption, landlord zero-dollar claim, tenant acceptance, and settlement. [Public deployment summary](evidence/SOLANA_STAGED_DEVNET_DEPLOYMENT_2026-09-24.json) records finalized slot `503502948`, closed escrow, zero cash/receipts, and 10,000,000 atomic test-USDC in the fixed tenant payout account. Privy-linked addresses and signed receipts are local only; this public summary is **not** independently replayable by itself. |
| A different Privy tenancy remains active | A separate joint-signature program has an active 10-test-USDC deposit supplied to Kamino, last read with 10,000,000 receipt units and zero idle cash. It is not the settled tenancy. Do not close or change it during review. |
| Solana program deployed as tested | Staged program `B1hjmapwssey8AbpjAtw5qF87DvvtuSisGov4kHec7Yc`, 454,752-byte SBF SHA-256 `29b7a9877bb8dacfa170ecf13e7c112a88d6dd3355e006b79975a25baefaae41`. Check finalized executable/ProgramData and upgrade authority against the [manifest](evidence/SOLANA_STAGED_DEVNET_DEPLOYMENT_2026-09-24.json) and [pinned SBF CI run](https://github.com/GiraeffleAeffle/rental-deposit-hackathon/actions/runs/35969371724). Upgrade authority is still present. |
| Robinhood path has native proof | The restricted Solidity escrow and Morpho flow passed local tests and a Gnosis/mainnet-code fork fixture. **No Robinhood Chain app deployment or connected wallet cycle** has been proved. |
| “Tenant to investor” is implemented | The portfolio demo, accounting, route validation, and Jupiter **price-only** quotes exist. No earned surplus, eligible xStock order, buy/sale fill, or investment-proceeds withdrawal is proved. Fixture returns and sample holdings must never be counted as real finance evidence. |

The [operator-key Solana rehearsal](SOLANA_DEVNET_REHEARSAL.md) is a third, distinct test, not the Privy-backed application proof. Local fixture tests use synthetic time/accrual. No real deposit product, guaranteed yield, legal approval, or production operation has been established.

## Verification sequence

1. Check `git status`, the pinned commits, PR diff, and current CI status. The last observed application, EVM, Solana-host, and staged-SBF checks passed; recheck rather than assuming they remain green. Run `npm test` (123 tests expected), `npm run lint`, and `git diff --check`. The app may already be serving on port 4175; avoid a concurrent `next build` against its `.next` directory. Use CI's production-build result or an isolated checkout for a fresh build. The repo's `AGENTS.md` requires reading `node_modules/next/dist/docs/` before changing Next.js code.
2. Trace custody and authority from on-chain program to API to wallet. Prioritize `programs/rental_escrow/src/{lib,state,accounting,lending}.rs`, `programs/rental_escrow/tests/svm.rs`, `src/finance/solana/{manifest,program,rpc,reconcile,inspection}.ts`, `src/server/{solana-initialization,solana-service,solana-rpc,agreements,recovery,store}.ts`, `src/wallets/{provider,signing-policy}.ts`, and `src/components/{solana-initialization,native-solana,solana-review}.tsx` (or `.ts` where present). Follow the relevant `app/api/` routes. Check role binding, accepted-term hash, fixed token recipients, program/cluster/code pinning, sponsor fee ceiling, nonce/idempotency, expiry, signed-byte persistence, ambiguous broadcast retry, finalized receipt/token deltas, loss/liquidity handling, and claim/settlement accounting. In particular, decide whether renewing an expired **unsigned** review proves the replacement has the same authorized action, deltas, fee ceiling, and deployment, while a **signed** review cannot be silently replaced.
3. Independently read devnet, if network access permits. Two ignored local descriptors exist: `.testnet-secrets/solana/exit-proof-manifest.json` for the **closed staged** tenancy and `.testnet-secrets/solana/joint-app-manifest-backup.json` for the **active joint** tenancy. Each has `tenancyAddress`, program and reserve IDs, and the network genesis hash; they contain wallet-linked identifiers, so do not paste their contents into the PR or review. `.env.local` has the RPC endpoint and credentials; do not print or commit it. Use read-only finalized `getGenesisHash`, `getAccountInfo`, and `getTokenAccountBalance` calls, or the existing `readTenancy` and `deriveEscrowAddresses` functions in `src/finance/solana/`. Check account owner/PDA, phase, required security, nonce, escrow cash/receipt balances, and the closed tenancy's immutable tenant/landlord payout accounts. Last observed: staged `closed`, nonce 6, 0 escrow cash, 0 receipts, tenant payout 10,000,000 atomic test-USDC, landlord payout 0; joint `active`, nonce 2, 0 idle cash, 10,000,000 receipt units. A later change in a personal payout account needs transaction-history investigation before declaring the earlier settlement false. If RPC is unavailable, report the chain check as **unverified**, not passed.
4. Check the EVM path in `contracts/evm/src/{RentalEscrow,MorphoAdapter}.sol`, `src/server/robinhood-service.ts`, and `src/finance/robinhood/`. Distinguish local/fork proofs from a Robinhood deployment. Review the investment adapters in `src/finance/{solana/jupiter,robinhood/zeroEx}.ts`, then inspect UI language for any suggestion that a quote, mock growth, or sample stock holding is real.
5. Assess whether the product journey is intelligible for tenant, landlord and arbitrator. The staged setup removed synchronized two-party signing, but native panels still expose dense technical details and manual result checks. Compare the displayed next action and recovery behavior with the actual server state. State whether this is a credible **hackathon demo** and what would block real-money production: independent security review, provider/instrument eligibility, measured yield and actual buy/sale/exit, hosted database and worker, backup/monitoring, and operating/legal arrangements.

## Output requested

Report findings first, ordered by severity, with exact file/line, a concrete failure path, and whether it is reproduced or a reasoned risk. Then list checks actually run (including the observed devnet slot or why chain verification was unavailable), claims confirmed, claims not confirmed, and a clear verdict for **hackathon demo** versus **production**. If you find no blocking issue, say that explicitly while retaining the unproved boundaries above. Suggest the next smallest implementation slice, but leave PR #1 draft and defer merge or real-money deployment to a separate decision.
